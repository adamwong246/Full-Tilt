=== This is a demo of the ERB template language. ===


<%# This is a comment. It should NOT appear in the compiled file. %> (there's a comment here but you can't see it! )

<%= "Just printing out a string" %>

A dump of the configurations:  <%= self %>

It can be accessed thus: <%= self['author'] %>

A new line test

<%= "The first line \n the second line" %>

A method test

DUMP
<%= self  %><


<%= self['colors'] %>

<%= colors.black %>