<%= render "src/partials/header.erb", {prefix: "<!-- ", suffix: ' -->'} %>
<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
  <title><%= render "src/partials/title.erb" %></title>
  <style>
	  <% colors_as_a_hash.each do |k, v| %>
			<%= ".#{k}{ color: ##{v}" %>
		<% end %>
  </style>
</head>
<body>
	<table>
		<caption>Every color, set against every other color</caption>
		<% colors_as_a_hash.each do |k, v| %>
			<tr >
				<% colors_as_a_hash.each do |kk, vv| %>
					<td style="color:#<%= "#{v}" %>;background-color:#<%= "#{vv}" %>;" >
						<p >
							<%= "#{kk}: #{vv}" %>
						</p>
					</td>
				<% end %>
			</tr>
		<% end %>	  
	</table>		
	<table>
		<caption>Every color, set against shades of gray</caption>
		<% (1..10).each do |i| %>
			<tr >
				<% colors_as_a_hash.each do |k, v| %>
					<td style="color:#<%= "#{v}" %>;background-color:<%= "#{black_white(i/10.to_f)}" %>;" >
						<p >
							<%= "#{k}: #{v}" %>
						</p>
					</td>
				<% end %>
			</tr>
		<% end %>	  
	</table>
</body>
</html>